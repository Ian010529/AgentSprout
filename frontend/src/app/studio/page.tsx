import { PlannedRoute } from "@/components/planned-route";

export default function StudioPage() {
  return (
    <PlannedRoute
      eyebrow="Student and Teacher Studio"
      title="The workshop opens in M2."
      description="The Dashboard will restore real server-owned Agents, roles, and next actions after the access boundary exists."
      module="M2 — Access and Dashboard"
    />
  );
}
