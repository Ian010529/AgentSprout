import { PlannedRoute } from "@/components/planned-route";

export default function AccessPage() {
  return (
    <PlannedRoute
      eyebrow="Protected Studio"
      title="Studio access begins in M2."
      description="The shared access-code session, secure cookie, and complete failure states will be built and verified together."
      module="M2 — Access and Dashboard"
    />
  );
}
